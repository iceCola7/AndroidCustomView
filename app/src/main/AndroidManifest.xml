<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cxz.androidcustomview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.BankCardActivity" />
        <activity android:name=".activity.PayPsdViewActivity" />
        <activity android:name=".activity.WaveViewActivity" />
        <activity android:name=".activity.LoadingActivity" />
        <activity android:name=".activity.WaveView2Activity" />
        <activity android:name=".activity.NumberKeyboardActivity" />
        <activity android:name=".activity.PwdKeyboardActivity" />
        <activity android:name=".activity.CircleIndicatorActivity" />
        <activity android:name=".activity.TiltTextViewActivity" />
        <activity android:name=".activity.RollTextViewActivity" />
        <activity android:name=".activity.JDPullRefreshActivity" />
    </application>

</manifest>